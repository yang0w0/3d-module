# SPEC —— 模块需求与架构规格

> 版本：v1（2026-09-05 定稿）　状态：已确认，待开发

## 1. 一句话定义

一个自包含的 HTML/JS 模块，提供「参考图 → 混元3D 生成 GLB → 浏览器内修复/定尺寸 → 导出可打印 STL」的完整流程，可独立打开使用，也可作为功能面板嵌入其他 HTML 工作台。

## 2. 功能清单（按面板划分）

### P1 生成面板
- 输入：参考图上传（拖拽/点击，jpg/png/webp，单边 128~5000px，≤6MB）
- 参数：模型版本（3.0/3.1）、GenerateType（默认 **Geometry 白模**，省积分）、FaceCount、ResultFormat（固定 GLB 用于预览 + STL 用于打印，一次生成双格式）
- 流程：提交 → 轮询状态（3~5s 一次）→ DONE 后下载 GLB/STL
- 生成历史：本地记录（图片缩略、JobId、时间、文件），Url 仅 24h 有效所以必须下载落盘
- 备用入口：混元官网（3d.hunyuan.tencent.com，每日免费额度）链接卡片，供 API 积分耗尽时使用

### P2 预览面板
- three.js 加载 GLB/STL（也支持直接拖入外部文件）
- 轨道旋转 / 缩放 / 线框切换 / 网格地面参照

### P3 修复面板
- 水密性检测（ manifold3d WASM 或边计数法）
- 一键修复（Make Manifold）：补洞、去非流形
- 修复前后对比（原模型 vs 修复结果切换显示）

### P4 导出面板
- 输入目标尺寸（如"高 50mm"），自动等比换算 XYZ
- 导出二进制 STL
- 打印提示文案（FDM 0.4mm 极限、空壳模型需加壁厚等）

### P5 设置面板
- API 密钥配置（见 API.md 安全规则）
- 积分余额提示 / 每次生成消耗提示（Normal 25 / Geometry 15 / LowPoly 30）

## 3. 架构分层

```
┌─────────────────────────────────────────────┐
│  UI 层（面板，可独立成页也可嵌入宿主）          │
├─────────────────────────────────────────────┤
│  生成服务适配层  │  网格处理层    │  存储层     │
│  hunyuan-       │  manifold-    │  localStorage│
│  client.js      │  fix.js       │  (历史/设置)  │
│  (提交/轮询/     │  (检测/修复/   │             │
│   TC3签名)      │   缩放/导STL)  │             │
└─────────────────────────────────────────────┘
```

- 各层为独立 JS 文件，UI 层是唯一可以被宿主工作台替换/嵌入的部分
- 生成服务适配层必须做到"可换源"：混元 API 是默认实现，接口留好，将来可换本地 Colab / TRELLIS 服务
- 已预留 provider 接口：除混元 API 外，可选择"本地开源服务"，按 `LOCAL_PROVIDER.md` 的 HTTP 契约接入 TripoSR / InstantMesh / Hunyuan3D / TRELLIS 等模型

## 4. 两种运行形态

| 形态 | 触发方式 | 说明 |
|---|---|---|
| 独立工作台 | 直接打开 `index.html` | 完整页面，第一阶段的交付物 |
| 嵌入面板 | 宿主页面 iframe 加载，或按 INTEGRATION.md 契约挂载 | 第二阶段的交付物 |

两种形态**共用同一套核心 JS**，区别只在入口/外壳。

## 5. 非目标（明确不做）

- ❌ 本地跑开源生成模型（无 N 卡多设备，堵死）
- ❌ 贴图/PBR 编辑（打印用不到）
- ❌ 切片出 gcode（交给 Cura 等专业切片软件）
- ❌ 服务端部署为长期运行的后端（除非用户选择"本地代理"密钥方案，见 API.md）
